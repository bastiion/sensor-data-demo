{
  "name": "sensor-data-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/sensor-map dev",
    "build": "bun run --cwd apps/sensor-map build",
    "lint": "bun run --cwd apps/sensor-map lint"
  },
  "dependencies": {

  },
  "devDependencies": {
    "vite-tsconfig-paths": "^5.1.0"
  }
}
